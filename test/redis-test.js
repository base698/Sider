var redisIndexer = require('../lib/sider')
	, chai = require('chai')
	, assert = chai.assert;

describe('redis',function() {
	before(function(done) {

	});

	it('Should add token to redis.',function(done) {
	});

	it('should add hash to redis.',function(done) {

	});

	after(function(done) {

	});

});
